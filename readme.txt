disaster engine version 5

c# sdl opengl game engine by jazz "danger" mickle

test: `dotnet run`
build: `dotnet build -c release`

no build system yet. external resources should all be in res/ so copy that folder in too, dunno if all the dlls automatically go in yet
